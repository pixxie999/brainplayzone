name: Deploy to Cloudways

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 141.164.61.123
          username: master_byqgfvytgg
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            find /home/master_byqgfvytgg -name "index.html" 2>/dev/null | head -5
            ls /home/master_byqgfvytgg/
